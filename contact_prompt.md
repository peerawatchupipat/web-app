! run test: "integration/contact_test.rb"
! verify tests fail

loop until tests pass do
  Build a view that does this: 
  
    Loop through all contacts
    Display:
      Contacts' full name
      Name of the contact's company
end